Refactored `DbFirstProductQueryHandlers.cs`

1. Extracted the duplicated 11-property projection into a shared 
   `ProductProjections.ToViewModel` — kept as an `Expression<Func<>>` rather 
   than a method/delegate, since a plain method inside `GroupBy().Select()` 
   would silently fall back to client-side evaluation in EF Core.
2. Split the file into one handler per file, matching the rest of the codebase's 
   convention.
3. Removed leftover 
4. Deliberately left the paging overflow bug and the LastUpdatedAt/Description
   AutoMapper divergence untouched — both are pinned by existing tests, and 
   "fixing" them here would have been an undisclosed behavior change disguised 
   as a readability refactor.

