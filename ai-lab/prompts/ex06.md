generate comprehensive API documentation for this project. Include:

- An API README overview (what the API does, tech stack, how to run it)
- A summary of each endpoint (route, method, purpose)
- Example requests for each endpoint (realistic payloads)
- Example responses for each endpoint (success and at least one error case)
- Architecture notes (CQRS/MediatR pattern, project structure, key design decisions)

Base this on the actual current endpoints and code in this project — don't 
invent routes that don't exist. Save the output to docs/ai-generated-api-docs.md