For Exercise 04, generate integration tests using .NET's WebApplicationFactory 
for these routes:

POST /api/products — full product initialization flow
POST /api/products/{id}/image — binary image stream upload
DELETE /api/products/{id} — resource teardown

For each route, write explicit assertions covering:
- HTTP response status code and relevant headers
- Response body/model properties matching what was sent or expected
- Actual persisted side effects (query the database/filesystem afterward to 
  confirm what really happened, not just the HTTP response)

Follow the existing testing conventions already used in this codebase (check 
how existing integration tests are structured — fixtures, WarehouseDbContext 
setup, etc.)