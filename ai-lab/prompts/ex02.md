    public void AssignSupplier(Supplier supplier)
    {
        if (IsArchived)
            throw new InvalidOperationException("Archived products cannot be updated.");

        if (!supplier.IsActive)
            throw new InvalidOperationException("Inactive suppliers cannot be assigned to products.");

        SupplierId = supplier.Id;
        SupplierName = supplier.Name;
        Supplier = supplier; 
        LastUpdatedAt = DateTime.SpecifyKind(DateTime.UtcNow, DateTimeKind.Unspecified);
    }

Here is my current AssignSupplier() method in ProductService.cs . There's a bug — archived/historical products can still be assigned to active suppliers, and they shouldn't be. Please: 1) explain the faulty logic in plain text, 2) point out the exact lines causing it, 3) give me a fixed C# version, 4) write a unit test that verifies assigning a supplier to an archived product throws an exception."


